<%= form_tag password_resets_path, :method => :post, class: 'horizontal-form' do %>
  <div class="field">
    <%= label_tag :email, t('users.email') %><br />
    <%= text_field_tag :email %> <%= submit_tag t('users.send_reset_password_email') %>
  </div>
<% end %>